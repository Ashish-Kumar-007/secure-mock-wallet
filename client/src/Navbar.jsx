import React from "react";

const Navbar = () => {
  return (
    <nav className="navbar">
      <img className="navbar__logo" src="logo.png" alt="secure-mock-wallet" />
    </nav>
  );
};

export default Navbar;
